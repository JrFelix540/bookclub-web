mutation CreateComment($postId: Float!, $content: String!) {
    createComment(postId: $postId, content: $content) {
        comment {
            id
            content
        }
        errors {
            field
            message
        }
    }
}
