query Community($id: Float!) {
    community(id: $id) {
        id
        name
        hasJoined
        memberIds
        dateCreated
        description
        creator {
            id
        }
    }
}
